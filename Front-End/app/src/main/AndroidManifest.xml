<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.E_sports_tournament_management_system"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashboardActivity" />
        <!-- 🔽 Activités de gestion des utilisateurs -->
        <activity android:name=".UsersActivity" />
        <activity android:name=".AddUserActivity" />
        <activity android:name=".EditUserActivity" />
        <!-- 🔽 Activités de gestion des jeux -->
        <activity android:name=".GamesActivity" />
        <activity android:name=".AddGameActivity" />
        <activity android:name=".EditGameActivity" />
        <!-- 🔽 Activités de gestion des Joueurs -->
        <activity android:name=".PlayersActivity" />
        <activity android:name=".AddPlayerActivity" />
        <activity android:name=".EditPlayerActivity" />
        <!-- 🔽 Activités de gestion des Equipes -->
        <activity android:name=".TeamsActivity" />
        <activity android:name=".AddTeamActivity" />
        <activity android:name=".EditTeamActivity" />
        <!-- 🔽 Activités de gestion des Matches -->
        <activity android:name=".MatchesActivity" />
        <activity android:name=".AddMatchActivity" />
        <activity android:name=".EditMatchActivity" />
        <!-- 🔽 Activités de gestion des Tournois -->
        <activity android:name=".TournamentsActivity" />
        <activity android:name=".AddTournamentActivity" />
<!--        <activity android:name=".EditTournamentActivity" />-->
        <activity android:name=".TournamentDetailsActivity" />



    </application>

</manifest>